<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation - Aura Events</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #000;
            color: #eee;
            min-height: 100vh;
            padding: 40px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .confirmation-card {
            background-color: #111;
            border: 1px solid #333;
            border-radius: 12px;
            padding: 40px;
            width: 100%;
            max-width: 800px;
            text-align: center;
            box-shadow: 0 4px 15px rgba(255, 193, 7, 0.2);
            animation: fadeIn 0.6s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .confirmation-header {
            color: #ffc107;
            margin-bottom: 30px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
        }

        .confirmation-header i {
            font-size: 32px;
        }

        .confirmation-header h2 {
            font-size: 28px;
            font-weight: bold;
        }

        .order-image {
            max-width: 300px;
            height: auto;
            border-radius: 8px;
            margin: 20px auto;
            border: 3px solid #ffc107;
            box-shadow: 0 4px 15px rgba(255, 193, 7, 0.3);
        }

        .order-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin: 30px 0;
            text-align: left;
        }

        .detail-item {
            padding: 15px;
            background-color: #222;
            border-radius: 8px;
            border: 1px solid #333;
            transition: transform 0.3s ease;
        }

        .detail-item:hover {
            transform: translateY(-2px);
        }

        .detail-item strong {
            color: #ffc107;
            display: block;
            margin-bottom: 5px;
            font-size: 14px;
            text-transform: uppercase;
        }

        .detail-item p {
            color: #eee;
            font-size: 16px;
            word-break: break-word;
        }

        .home-btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background-color: #ffc107;
            color: #000;
            border: none;
            padding: 14px 28px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
            font-size: 16px;
            margin-top: 30px;
            transition: all 0.3s ease;
        }

        .home-btn:hover {
            background-color: #ffca2c;
            transform: scale(1.05);
        }

        @media (max-width: 768px) {
            .confirmation-card {
                padding: 25px;
            }

            .confirmation-header h2 {
                font-size: 24px;
            }

            .order-image {
                max-width: 250px;
            }

            .detail-item {
                padding: 12px;
            }

            .home-btn {
                padding: 12px 24px;
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <div class="confirmation-card">
        <div class="confirmation-header">
            <i class="fas fa-check-circle"></i>
            <h2>Booking Confirmed!</h2>
        </div>

        <img class="order-image" src="/uploads/<%= order.image %>" alt="Event Image">

        <div class="order-details">
            <div class="detail-item">
                <strong>Program Name</strong>
                <p><i class="fas fa-calendar-day"></i> <%= order.program_name %></p>
            </div>

            <div class="detail-item">
                <strong>Event Date</strong>
                <p><i class="fas fa-calendar"></i> <%= new Date(order.date).toLocaleDateString() %></p>
            </div>

            <div class="detail-item">
                <strong>Event Time</strong>
                <p><i class="fas fa-clock"></i> <%= order.time %></p>
            </div>

            <div class="detail-item">
                <strong>Address</strong>
                <p><i class="fas fa-map-marker-alt"></i> <%= order.address %></p>
            </div>

            <div class="detail-item">
                <strong>Pincode</strong>
                <p><i class="fas fa-map-pin"></i> <%= order.pincode %></p>
            </div>

            <div class="detail-item">
                <strong>Description</strong>
                <p><i class="fas fa-info-circle"></i> <%= order.description %></p>
            </div>

            <div class="detail-item">
                <strong>Booking Date</strong>
                <p><i class="fas fa-calendar-check"></i> <%= new Date(order.booking_date).toLocaleString() %></p>
            </div>

            <div class="detail-item">
                <strong>Phone Number</strong>
                <p><i class="fas fa-phone"></i> <%= order.phonenumber %></p>
            </div>

            <div class="detail-item">
                <strong>Username</strong>
                <p><i class="fas fa-user"></i> <%= order.username %></p>
            </div>
        </div>

        <a href="/user/home" class="home-btn">
            <i class="fas fa-home"></i> Return to Home
        </a>
    </div>
</body>
</html>