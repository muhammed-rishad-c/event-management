<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Specified List - Aura Events</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background-color: #000;
            color: #eee;
            font-family: 'Montserrat', sans-serif;
            margin: 0;
            padding: 0;
        }

        .navbar {
            background-color: #111;
            padding: 1rem 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: 0 4px 6px rgba(255, 193, 7, 0.2);
        }

        .navbar-left {
            display: flex;
            align-items: center;
            margin-left: -1.5rem;
        }

        .navbar .brand {
            font-size: 2rem;
            font-weight: bold;
            color: #ffc107;
            margin-right: 2rem;
        }

        .navbar a, .navbar button {
            color: #ffc107;
            text-decoration: none;
            font-size: 1.2rem;
            margin: 0 1rem;
            transition: all 0.3s ease-in-out;
            background: none;
            border: none;
            cursor: pointer;
            padding: 0.5rem 1rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .navbar a i, .navbar button i {
            font-size: 1.3rem;
        }

        .navbar a:hover, .navbar button:hover {
            color: #fff;
            transform: scale(1.1);
        }

        .back-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 0.5rem 1rem;
            background: none;
            color: #ffc107;
            font-size: 1.2rem;
            font-weight: bold;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            margin: 1rem 2rem;
            max-width: 200px;
        }

        .back-btn:hover {
            color: #fff;
            transform: scale(1.1);
        }

        .container {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            width: 90%;
            max-width: 1200px;
            margin: auto;
        }

        .card {
            background: #222;
            border-radius: 10px;
            padding: 10px;
            text-align: center;
            color: #ffc107;
            text-decoration: none;
            transition: transform 0.3s, box-shadow 0.3s;
            box-shadow: 0px 4px 12px rgba(255, 193, 7, 0.2);
            position: relative;
            overflow: hidden;
        }

        .card:hover {
            transform: translateY(-3px);
            box-shadow: 0px 8px 16px rgba(255, 193, 7, 0.3);
        }

        .card img {
            max-width: 100%;
            border-radius: 10px;
            margin-bottom: 10px;
            transition: transform 0.3s;
        }

        .card:hover img {
            transform: scale(1.02);
        }

        .card h2 {
            font-size: 16px;
            font-weight: 600;
            margin: 10px 0;
            color: #ffc107;
        }

        .card p {
            font-size: 14px;
            color: #eee;
            margin: 5px 0;
        }

        .card .fas.fa-map-marker-alt {
            color: red;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            }
            .back-btn {
                font-size: 14px;
                padding: 10px 20px;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="navbar-left">
            <a href="#" class="brand"><i class="fas fa-crown"></i> Aura Events</a>
            <a href="/user/home"><i class="fas fa-home"></i> Home</a>
            <a href="/user/booking-history"><i class="fas fa-history"></i> Check Orders</a>
            <a href="/user/profile"><i class="fas fa-user"></i> Profile</a>
        </div>
        <div>
            <button onclick="window.location.href='/user/logout'"><i class="fas fa-sign-out-alt"></i> Logout</button>
        </div>
    </nav>

    <button class="back-btn" onclick="window.location.href='/user/home'"><i class="fas fa-arrow-left"></i> Back to Home</button>

    <div class="container">
        <% admin.forEach(function(element) { %>
            <a href="/user/photographer-detail/<%= element.username %>" class="card">
                <img src="/uploads/<%= element.photo %>" alt="<%= element.fullname %>">
                <h2><%= element.fullname %></h2>
                <p><i class="fas fa-map-marker-alt"></i> <%= element.district %></p>
                <p><i class="fas fa-dollar-sign"></i> Price: <%= element.price %></p>
            </a>
        <% }); %>
    </div>

</body>
</html>
